
  


<div class="list-group">
    @foreach (var chude in Model)
    {
    <a href="@Url.Action("Sptheobrand","ShoeStore", new { id = chude.MaCD })" class="list-group-item">  @chude.TenChuDe</a>

    }
    </div>